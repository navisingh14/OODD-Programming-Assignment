<br>
<h2>Rooms</h2>
<% provide(:title, 'All rooms') %>
<ul class="rooms">
  <% @rooms.each do |room| %>
      <li>
        <%= link_to room.number, room %>
      </li>
  <% end %>
</ul>
<%= link_to "Add a Room", room_add_path %>